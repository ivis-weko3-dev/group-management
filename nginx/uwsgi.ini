[uwsgi]
base = /group-management/module
app = app

socket = /var/run/uwsgi.sock
chmod-socket = 666
vacuum = true

wsgi-file = %(base)/app.py
callable = app

touch-reload = /group-management/nginx/uwsgi.ini

master = true

log-format = %(addr) - %(user) [%(ltime)] "%(method) %(uri) %(proto)" %(status) %(size) "%(referer)" "%(uagent)" %(msecs)