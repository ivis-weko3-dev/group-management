server {
    listen 443 ssl;

    ssl_certificate "/etc/nginx/cert/server.crt";
    ssl_certificate_key "/etc/nginx/cert/server.key";

    server_name jairo-example.com;

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/var/run/uwsgi.sock;
    }
}